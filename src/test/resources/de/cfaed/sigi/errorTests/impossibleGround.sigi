
let loop: int, int -> =
    -> i, running;
    if (i = 0) pass
#   ^ error: Mismatched types: 'S\d+ is not compatible with 'S\d+, int
    else
        i pp
        (i - 1) (running * 10) loop;;

10 1 loop
